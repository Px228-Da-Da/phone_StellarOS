#ifndef UI_H
#define UI_H
#include "types.h"

/*
 * Первый экран VELO-OS.
 *
 * Устроен так, чтобы показать ровно то, что мы умеем, и не притворяться
 * большим, чем есть: плитки со сведениями о машине, подсветка нажатой
 * плитки и точка, следующая за пальцем.
 *
 * Важное в устройстве: подсветка и точка — это не рисование, а аппаратные
 * слои. Нажали на плитку — записали её координаты в регистр слоя, и он
 * появился там; отпустили — выключили слой. Ни одного пикселя при этом не
 * переписывается, сколько бы пальцев ни возили по экрану.
 *
 * Перерисовывается только то, что изменилось: фон и плитки рисуются один
 * раз при запуске. Заливка всего экрана стоит 836 микросекунд, двадцатую
 * часть кадра, и делать её каждый кадр незачем.
 */

/* Запустить оболочку: создаёт свою задачу в планировщике */
void ui_start(void);

#endif
