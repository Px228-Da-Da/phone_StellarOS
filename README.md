# VELO-OS — своя ОС на своём ядре для Redmi Note 9 (merlin)

Bare-metal ядро AArch64, которое грузится вместо Linux на MediaTek Helio G85.

**Статус: этапы 1 и 3 пройдены.** Ядро собирается, стартует в QEMU, спускается в EL1,
включает MMU с кэшами, поднимает GICv3, тикает по прерываниям от таймера,
распоряжается всей физической памятью и выделяет блоки через kmalloc.

```
================================
  VELO-OS v0.3
  AARCH64 BARE METAL KERNEL
================================

EL       : 1
MPIDR    : 0000000080000000 (CORE 0)
CNTFRQ   : 62500000 HZ
DTB SIZE : 1048576 BYTES, VER 17
PMM      : 1024 МБ, СТРАНИЦ 262144, ЗАНЯТО 660, КАРТА 0x000000004008c000 (32 КБ)
КУЧА     : 12 КБ У PMM, БЛОКОВ 1, ЗАНЯТО 0
ТЕСТ     : ПАМЯТЬ РАБОТАЕТ
GIC      : GICv3, GICD 0x0000000008000000, GICR 0x00000000080a0000
TIMER    : 100 ГЦ, ИНТЕРВАЛ 625000 ТИКОВ, PPI 27
IRQ      : РАЗРЕШЕНЫ

BOOT OK. HEARTBEAT:
TICK 1  IRQ 100  UPTIME 1015 МС
TICK 2  IRQ 200  UPTIME 2032 МС
```

Ровно 100 прерываний на секунду — счётчик IRQ показывает, что тики идут от
контроллера прерываний, а не от опроса в цикле. Между тиками процессор спит в `wfi`.

---

## Как это работает

Мы **не пишем всё с нуля до последнего транзистора** — это невозможно в одиночку.
Мы встраиваемся в существующую цепочку загрузки MediaTek на последнем звене:

```
BROM -> preloader -> ATF -> LK -> [ЗДЕСЬ МЫ]
```

К моменту, когда управление доходит до нас, preloader уже настроил и откалибровал
LPDDR4X, а LK уже включил экран. Нам достаётся работающая память и работающая
панель — остаётся написать саму ОС. Подробно: [docs/03-boot-chain.md](docs/03-boot-chain.md).

## Быстрый старт

```bash
# один раз: инструменты в WSL
wsl -u root bash /mnt/d/OS_ANDROID/tools/setup-wsl.sh

# каждый день: сборка и запуск в эмуляторе
wsl -u root bash -c 'cd /mnt/d/OS_ANDROID/kernel && make BOARD=qemu && make run'
```

Ctrl+A затем X — выйти из QEMU.

## Структура

```
kernel/src/boot.S      точка входа: Image header, парковка ядер, EL2->EL1, стек
kernel/src/vectors.S   таблица векторов исключений (без неё падение = чёрный экран)
kernel/src/main.c      kmain, диагностика, обработчик паники
kernel/src/mmu.c       таблицы трансляции, единичное отображение, кэши
kernel/src/pmm.c       физические страницы: битовая карта на всю DRAM
kernel/src/kmalloc.c   куча: блоки произвольного размера, дробление и склейка
kernel/src/gic.c       GICv3: редистрибьютор, дистрибьютор, ICC_*, разбор прерываний
kernel/src/timer.c     ARM Generic Timer: периодический тик ядра
kernel/src/fb.c        фреймбуфер: находим буфер LK и пишем в него пиксели
kernel/src/uart.c      PL011 (QEMU) и MTK 8250 (телефон)
kernel/src/print.c     kprintf — печатает сразу и в UART, и на экран
kernel/src/soc/        карты регистров MT6768 и QEMU virt
tools/                 setup, упаковка boot.img, прошивка и откат
docs/                  устройство, безопасность, разблокировка, цепочка загрузки
```

## Три ключевых решения проекта

**1. Всё сначала в QEMU.** Каждая фича доводится в эмуляторе и только потом
едет на телефон. Один цикл «прошил — чёрный экран — откатил» стоит 10 минут,
цикл в QEMU — 10 секунд.

**2. Эмулятор настроен как телефон.** QEMU запускается с `gic-version=3`, потому что
в merlin стоит GIC-500 — это GICv3. Версия по умолчанию (v2) устроена принципиально
иначе, и отлаженный на ней код на телефоне пришлось бы писать заново.

**3. Экран вместо UART.** До UART на этом телефоне нужно паяться к тест-пойнтам.
Вместо этого мы читаем адрес фреймбуфера прямо из регистра контроллера дисплея
(его настроил LK, когда рисовал логотип) и пишем туда текст сами. `kprintf`
выводит в оба канала, так что один и тот же код отлаживается везде.

---

## Дорожная карта

### Этап 1 — ядро оживает ✅ ГОТОВО
- [x] Заголовок arm64 Image, вход по протоколу Linux boot
- [x] Парковка вторичных ядер, спуск EL2 → EL1, стек, .bss
- [x] Таблица векторов и печать паники с ESR/ELR/FAR
- [x] UART (PL011 + MTK), kprintf, шрифт 8x8, консоль на фреймбуфере
- [x] Сборка под QEMU и под merlin, запуск в эмуляторе

### Этап 2 — попасть на железо
- [ ] Разблокировать загрузчик ([docs/02-unlock.md](docs/02-unlock.md), 7 дней ожидания)
- [ ] Снять полный бэкап разделов через mtkclient ([docs/01-safety.md](docs/01-safety.md))
- [ ] Вытащить merlin.dts из ядра Xiaomi и сверить все `TODO-VERIFY` в `soc/mt6768.h`
- [ ] Первая прошивка: увидеть на экране телефона текст, написанный нашим кодом

### Этап 3 — ядро становится настоящим ✅ ГОТОВО
- [x] MMU: таблицы трансляции, включение кэшей D+I
- [x] **Драйвер GICv3, включение прерываний**
- [x] **ARM Generic Timer как источник тиков**
- [x] **Физический аллокатор страниц и куча (kmalloc)**

Каждая подсистема памяти проверяет себя при загрузке: страница приходит обнулённой,
блоки кучи не перекрываются, соседние свободные блоки склеиваются, после серии
выделений вразнобой ничего не течёт. Отчёт печатается на экран — на телефоне
это будет единственный доступный способ узнать, что всё цело.

### Этап 4 — многозадачность
- [ ] Запуск остальных 7 ядер через PSCI (`smc CPU_ON`)
- [ ] Контекст задачи, вытесняющий планировщик
- [ ] Спинлоки и атомарные операции

### Этап 5 — устройства
- [ ] Разбор DTB по-настоящему (поиск узлов, чтение reg/interrupts)
- [ ] I2C и драйвер тачскрина — первое устройство ввода
- [ ] Кнопки громкости/питания через PMIC
- [ ] eMMC: чтение разделов

### Этап 6 — userspace
- [ ] EL0, системные вызовы, раздельные адресные пространства
- [ ] Простая графическая оболочка на фреймбуфере

Этапы 1–4 — это фундамент настоящей ОС. Этапы 5–6 бесконечны: ровно так же
устроена работа над любым ядром, включая Linux.

## Что честно сказать про границы

Модем, Wi-Fi, Bluetooth, камера и GPU на MediaTek закрыты и не документированы.
Полноценной заменой Android эта система не станет. Целью стоит другое:
собственное работающее ядро на настоящем железе — с графикой, вводом,
многозадачностью и многоядерностью.

Откат к заводскому Android — одна команда: `tools/flash.sh --restore`.
